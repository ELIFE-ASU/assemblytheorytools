# Install from the repository root:
#     python -m pip install -r docs/requirements.txt
#
# Local project paths are relative to the working directory. Installing the
# docs extra also supplies the runtime dependencies needed by autodoc, with
# pyproject.toml as the single source of dependency declarations.
.[docs]
