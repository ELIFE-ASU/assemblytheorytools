graft assemblytheorytools/precompiled
graft assemblytheorytools/data

# The test suite ships in the sdist only; it is not packaged into the wheel.
graft tests

global-exclude *.py[cod]
prune tests/__pycache__
